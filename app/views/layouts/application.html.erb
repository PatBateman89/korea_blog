<!DOCTYPE html>
<html>
  <head>
    <title>KoreaBlog</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
<div class="container">
  <header class="blog-header py-3">
    <div class="row">
      <div class="col-12 text-center logo">
        Korea Job Board
      </div>
    </div>
  </header>

  <hr>

  <nav class="py-1 mb-2">
    <nav class="nav d-flex justify-content-between">
      <%= link_to "Jobs", root_path, :class=> "p-2 text-muted" %></a>
      <%= link_to "Post A Job", new_post_path, :class=> "p-2 text-muted" %></a>
      <%= link_to "Articles", articles_path, :class=> "p-2 text-muted" %></a>
      <%= link_to "Contact", pages_contact_path, :class=> "p-2 text-muted" %></a>
      <%= link_to "ESL Lessons", pages_esl_lessons_path, :class=> "p-2 text-muted" %></a>
      <% if current_user %>
      <a> <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => "btn btn-sm btn-outline-secondary m-2 px-5" %> </a>
      <a><%= link_to "Edit Profile", edit_user_registration_path, :class => "btn btn-sm btn-outline-secondary m-2 px-5" %></a>
      <% else %>
      <a> <%= link_to "Log In", new_user_session_path, :class => "btn btn-sm btn-outline-secondary m-2 px-5" %> </a>
      <a><%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-sm btn-outline-secondary m-2 px-5" %></a>
      <% end %>
    </nav>
  </nav-scroller>
</div>

<div class="container">
    <%= render 'layouts/alerts' %>
    <%= yield %>
</div>
  </body>
</html>
